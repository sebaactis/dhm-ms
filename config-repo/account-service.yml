# Configuración para account-service

server:
  port: 8082

spring:
  application:
    name: account-service

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: public

# JWT Configuration (debe coincidir con user-service)
jwt:
  secret: YWJjZGVmZ2hpamtsbW5vcHFyc3R1dnd4eXoxMjM0NTY3ODkwYWJjZGVmZ2hpamtsbW5vcA==
  expiration: 3600000

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.value}

logging:
  level:
    com.dmh.accountservice: DEBUG

account:
  cvu:
    length: 22
  alias:
    words-file: classpath:words.txt
